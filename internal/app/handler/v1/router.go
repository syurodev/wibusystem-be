package v1

import (
	"github.com/gin-gonic/gin"
)

// RegisterRoutes đăng ký tất cả các routes cho API version 1.
func RegisterRoutes(group *gin.RouterGroup) {
	// userHandler.RegisterRoutes(group.Group("/users"))
	// Thêm các module khác ở đây
}
